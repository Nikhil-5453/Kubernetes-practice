## sample service.yml for LoadBalancer for application ingress

apiVersion: v1
kind: Service
metadata:
  name: zomato-svc
spec:
  type: LoadBalancer
  selector:
    app: zomato
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000